// import mapper from 'automapper-js';
import Candidate from './models/candidates/candidate.js';

class MessagesBusiness {
    constructor({}) {
        // this._CandidatesRepository = CandidatesRepository;
    }

    async getAnswer(params) {
        // const candidate = await this._CandidatesRepository.getCandidate(params);
        // return mapper(Candidate,candidate)
    }
}

export default MessagesBusiness;